OJO. Para cambiar el usuario de un PC ir a credenciales.
OJO. Para generar un repositorio.
1. Entramos en la carpeta.
2.1. Creamos repositorio
    - git init
2.2. Clonar un repositorio
    - git clone "url"
2.3. Encontrarnos en el repositorio
    - ...
3. Ver el estado del repositorio.
    - git status.
4. Para preparar los documentos modificados.
    - git add "nombreArchivo"
5. Para hacer commit
    -     git commit -m "comentario"
6. Para subir a github:
6.1 No esta especificada la url(primera vez):
    - git push "url"
    - git push -u origin "url"
    - git push -u origin "rama"
6.2. Esta especificada la url:
    - git push
